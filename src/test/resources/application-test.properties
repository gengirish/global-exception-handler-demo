# Test properties for Global Exception Handler Demo
# Server configuration for tests
server.port=8081

# Logging configuration for tests
logging.level.org.springframework=INFO
logging.level.com.example.exceptionhandler=DEBUG

# Spring application name for tests
spring.application.name=GlobalExceptionHandlerDemoTest
